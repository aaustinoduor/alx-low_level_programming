#include "main.h"
/**
 * _atoi - Convert string to numbers
 *
 * Return: n
 */
int _atoi(void)
{
	/* int j; */
	/* char arr[]; */

	/* while (*s) */
	/* { */
	/* j++; */
	/* if (*s >= 48 && *s <= '9') */
	/* conv[i] = *s */
	/* } */

	return (0);
}
