#include <stdio.h>

/**
 * Main - Entry
 *
 * Return Always 0 (success)
 */

int main(void)
{
int lowerCase = 'a';
while(lowerCase <= 'z')
{
if (lowerCase == 'a' || lowerCase 'q')
{
lowerCase += 1;
}
else
{
putchar(lowerCase);
lowerCase += 1;
}
}
putchar('\n');
return (0);
}
